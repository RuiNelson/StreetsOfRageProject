interface:
  display_name: "Mega Drive Remote"
  short_description: "Control, inspect, and improve Mega Drive automation"
  default_prompt: "Use $megadrive-remote to inspect and control the running MegaDriveEnvironment application, then generalize verified repeated workflows into the skill."
